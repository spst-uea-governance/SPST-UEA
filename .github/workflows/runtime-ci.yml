name: Runtime CI

on:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Intentional context-only pass
        run: echo "This branch intentionally replaces Runtime CI to test whether context-only protection detects workflow identity drift."
