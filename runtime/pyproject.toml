[build-system]
requires=["setuptools>=68","wheel"]
build-backend="setuptools.build_meta"

[project]
name="spst-runtime"
version="0.1.1"
description="Reference runtime for SPST-UEA"
license={text="Apache-2.0"}
requires-python=">=3.11"
dependencies=[
  "pydantic>=2",
  "fastapi>=0.115",
  "cryptography>=43"
]

[project.optional-dependencies]
dev=[
  # Verification tools are exact pins because Runtime CI is a normative gate.
  # Upgrades must be deliberate and revalidated on both Windows and Linux.
  "pytest==9.0.3",
  "pytest-asyncio==1.3.0",
  "coverage[toml]==7.13.5",
  "ruff==0.15.13",
  "mypy==1.20.1"
]

[project.scripts]
spst-runtime="spst_runtime.cli:main"

[tool.setuptools]
package-dir={""="."}

[tool.setuptools.packages.find]
include=["spst_runtime*"]

[tool.pytest.ini_options]
testpaths=["tests"]
addopts=["--strict-config", "--strict-markers"]
markers=[
  "conformance: marks tests for SPST-UEA RFC normative conformance"
]

[tool.ruff]
line-length=100

[tool.mypy]
python_version="3.11"
check_untyped_defs=true
warn_unused_configs=true

[tool.coverage.run]
branch=true
source=["spst_runtime"]
parallel=true
patch=["subprocess"]

[tool.coverage.report]
fail_under=80
show_missing=true
skip_covered=true
